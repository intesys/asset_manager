= semantic_form_for [:admin, resource], :multipart => true, :namespace => 'post' do |f|
  -#= f.error_messages
  = f.inputs do
    = f.input :title
    = f.input :sub_title

    -# TODO - helper di collegamento al media manager, per ogni campo
    -# che ho definito nel model che è "attaccatto" all'asset

  = f.actions
