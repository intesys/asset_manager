- unless collection.empty?
  .resources.asset_manager_asset_instances
    - collection.each do |resource|
      = render partial: 'asset_manager/asset_instances/asset_instance', locals: { resource: resource }
